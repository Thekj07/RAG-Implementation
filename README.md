# RAG-Implementation
This project implements a Retrieval-Augmented Generation (RAG) pipeline that supports both PDF files and directly provided text documents. It generates vector embeddings using Sentence Transformers and stores them in a FAISS vector database for efficient semantic search.
